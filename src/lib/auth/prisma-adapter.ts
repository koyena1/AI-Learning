import type { PrismaClient } from "@prisma/client";

// Placeholder for PrismaAdapter if needed later.
// NextAuth/Auth.js v5 supports various adapters; for this assignment we use JWT strategy + Prisma for storage.

export type PrismaAdapterLike = PrismaClient;

