# TODO

- [x] Create protected dashboard route (`src/app/dashboard/page.tsx`).
- [x] Fix TypeScript build blocker in `src/components/ui/use-toast.tsx`.
- [ ] Update Prisma Resource model to required spec.
- [ ] Create Prisma migration.
- [ ] Update dashboard queries to use new Resource fields (`type`, `notes`, `summary`).
- [ ] Run `npm run build` to confirm zero errors.

